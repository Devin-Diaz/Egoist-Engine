{
  "name": "egoist-engine",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "cd backend && mvn clean install && cp -r ../frontend/build/* src/main/resources/static/"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "dependencies": {}
}